# 课程卡片交互设计文档 (CourseBlock)

本文档详细说明了 `CourseBlock.vue` 组件的点击放大（原地展开）交互设计及调试笔记。

## 1. 核心设计目标
- **原地放大**: 卡片在点击后以原始位置为基准向四周扩张（约 1.6 倍宽，1.5 倍高）。
- **内容全显**: 展开状态下取消文字截断（Ellipsis），允许标题和地点换行显示。
- **主题适配**: 背景色与边框色通过 `color-mix` 自动匹配当前主题的亮/暗模式及背景色。
- **视觉聚焦**: 展开时层级提升，并伴随深阴影（Shadow）和强毛玻璃效果（Backdrop Blur）。

## 2. 交互逻辑实现
### 展开 (Expansion)
- **触发**: 点击卡片触发 `toggleExpand`。
- **层级**: `z-index` 瞬间从 `1` (或 hover 时的 `5`) 提升至 `100`。
- **曲线**: 使用 `cubic-bezier(0.34, 1.56, 0.64, 1)`。这是一种“回弹”曲线，使放大过程带有轻微的弹性感。
- **布局**: 放弃 CSS `scale` (会导致文字模糊且无法改变实际占用空间)，改为修改 `top`, `left`, `width`, `height` 属性。

### 收起 (Shrinking)
- **触发**: 再次点击卡片或点击页面空白处（通过全局 `window` 监听器）。
- **层级回收**: 关键点在于 `transition: z-index 0s 0.3s`。这意味着 `z-index` 的降级会在动画完成后（0.3秒延迟）才执行，确保卡片缩回过程中始终浮在其他卡片之上。
- **曲线**: 使用 `cubic-bezier(0.4, 0, 0.2, 1)`。平滑收回，确保归位精准。

## 3. Debug 笔记：已解决的冲突 (The "Ghosting" Issue)
在迭代过程中，我们遇到了严重的“叠影”和“动画中断”问题，主要原因及解决方案如下：

### A. Hover 冲突
- **现象**: 之前的卡片在 Hover 时有 `translateY(-2px)` 的位移。当卡片处于展开态或正在缩回时，Hover 触发的位移会叠加在布局动画上，产生“残影”。
- **方案**: 在 `is-expanded` 状态下强制禁用所有 `transform` 效果，并完全移除旧的 `hover` 抬升逻辑。

### B. z-index 穿插
- **现象**: 缩回动画进行到一半时，卡片突然由于层级降低被旁边的卡片遮挡。
- **方案**: 引入 `transition-delay`。让层级在动画结束后再恢复，维持“始终在上”的视觉逻辑。

### C. height: auto 的陷阱
- **现象**: `height: auto` 无法产生 CSS 过渡动画。
- **方案**: 采用计算后的固定数值（如 `expandedHeight = Math.max(baseHeight * 1.5, 120)`），确保每一像素的运动都是可插值的。

## 4. 当前已知特性
- 展开状态下高度是动态计算的，确保至少能容纳两行标题和详细地点。
- 使用了 `color-mix` 确保在极简白、深海蓝等任何预设下，卡片背景均能与底层壁纸形成合理的对比度。
